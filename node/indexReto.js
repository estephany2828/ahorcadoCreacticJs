const paises = require('./paises');
console.log(paises.Alemania().saludo)

var seleccior= (pais)=>{
    console.log('entro')

}

const readline = require('readline');
const rl = readline.createInterface({
input: process.stdin,
output: process.stdout
});

    rl.question('Escriba tu nombre ? ', (a) => {
    rl.question('Escribe el nro del pais', (b) =>{
    var c = parseInt(a)+parseInt(b);
    switch(b) {
        case x:
            saludo=
            break;
        case y:
            code block
            break;
        default:
            code block
    }
    console.log('La suma es: '+c);
    rl.close();
    });
    });
